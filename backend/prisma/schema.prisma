generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DIRECT_URL")
}

model Tenant {
  id                              String                  @id @default(uuid())
  name                            String
  slug                            String                  @unique
  category                        BusinessCategory
  status                          TenantStatus            @default(PENDING)
  logoUrl                         String?
  address                         String?
  phoneNumber                     String?
  email                           String?
  primaryColor                    String?
  secondaryColor                  String?
  websiteUrl                      String?
  description                     String?
  createdAt                       DateTime                @default(now())
  updatedAt                       DateTime                @updatedAt
  freeBranchOverride              Int                     @default(0)
  onboardingCompletedAt           DateTime?
  ownerPasswordChanged            Boolean                 @default(false)
  paidModeEnabled                 Boolean                 @default(false)
  freeUnitsLimit                  Int                     @default(1)
  trialDurationDays               Int                     @default(28)
  emailNotificationsEnabled       Boolean                 @default(true)
  welcomeEmailEnabled             Boolean                 @default(true)
  adminAlertEmailEnabled          Boolean                 @default(true)
  tenantNotificationEmailEnabled  Boolean                 @default(false)
  tenantSignupNotificationEnabled Boolean                 @default(true)
  digestFrequency                 String?
  adminEmailRecipients            String[]
  authEvents                      AuthEvent[]
  branches                        Branch[]
  businessUnits                   BusinessUnit[]
  coffeeCustomerProfiles          CoffeeCustomerProfile[]
  customerTransactions            CustomerTransaction[]
  emailLogs                       EmailLog[]
  emailTemplates                  EmailTemplate[]
  gymMemberProfiles               GymMemberProfile[]
  gymMemberSubscriptions          GymMemberSubscription[]
  gymMembershipPlans              GymMembershipPlan[]
  gymUserBranches                 GymUserBranch[]
  platformRevenues                PlatformRevenue[]
  terminals                       Terminal[]
  users                           User[]
}

model Branch {
  id                     String                   @id @default(uuid())
  tenantId               String
  name                   String
  address                String?
  phoneNumber            String?
  email                  String?
  isActive               Boolean                  @default(true)
  isMainBranch           Boolean                  @default(false)
  dailyWalkInAmount      Int                      @default(100)
  branchData             Json?
  createdAt              DateTime                 @default(now())
  createdBy              String?
  updatedAt              DateTime                 @updatedAt
  deletedAt              DateTime?
  deletedBy              String?
  createdByUser          User?                    @relation("BranchCreatedBy", fields: [createdBy], references: [id])
  deletedByUser          User?                    @relation("BranchDeletedBy", fields: [deletedBy], references: [id])
  tenant                 Tenant                   @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  cards                  Card[]
  dailyEntries           DailyEntry[]
  events                 Event[]
  primaryMemberProfiles  GymMemberProfile[]       @relation("PrimaryBranchProfiles")
  gymMemberSubscriptions GymMemberSubscription[]
  gymUserBranches        GymUserBranch[]
  inventoryCards         InventoryCard[]
  pendingAssignments     PendingMemberAssignment?
  subscriptions          Subscription[]
  terminals              Terminal[]

  @@unique([tenantId, name])
  @@index([deletedAt])
}

model BusinessUnit {
  id                String             @id @default(uuid())
  tenantId          String
  name              String
  unitType          BusinessUnitType   @default(LOCATION)
  address           String?
  phoneNumber       String?
  email             String?
  isActive          Boolean            @default(true)
  isPaid            Boolean            @default(false)
  trialEndsAt       DateTime?
  subscriptionTier  String?            @default("basic")
  monthlyPrice      Decimal?           @default(3999) @db.Decimal(8, 2)
  businessUnitData  Json?
  settings          Json?
  createdAt         DateTime           @default(now())
  updatedAt         DateTime           @updatedAt
  tenant            Tenant             @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  saasSubscriptions SaasSubscription[]

  @@unique([tenantId, name])
  @@index([tenantId])
  @@index([unitType])
  @@index([isPaid])
  @@index([trialEndsAt])
}

model Plan {
  id            String         @id @default(uuid())
  name          String         @unique
  price         Decimal
  billingCycle  BillingCycle
  description   String?
  isActive      Boolean        @default(true)
  subscriptions Subscription[]
}

model Subscription {
  id        String             @id @default(uuid())
  branchId  String
  planId    String
  startDate DateTime
  endDate   DateTime
  status    SubscriptionStatus @default(ACTIVE)
  createdAt DateTime           @default(now())
  updatedAt DateTime           @updatedAt
  payments  Payment[]
  branch    Branch             @relation(fields: [branchId], references: [id], onDelete: Cascade)
  plan      Plan               @relation(fields: [planId], references: [id])
}

model Payment {
  id             String        @id @default(uuid())
  subscriptionId String
  amount         Decimal
  paymentDate    DateTime      @default(now())
  status         PaymentStatus @default(PENDING)
  paymentMethod  String?
  transactionId  String?       @unique
  receiptUrl     String?
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt
  subscription   Subscription  @relation(fields: [subscriptionId], references: [id], onDelete: Cascade)
}

model SystemSettings {
  id                         String                @id @default("system")
  passwordSecurityLevel      PasswordSecurityLevel @default(MEDIUM)
  globalAdminEmails          String[]
  newTenantAlertsEnabled     Boolean               @default(true)
  systemAlertsEnabled        Boolean               @default(true)
  securityAlertsEnabled      Boolean               @default(true)
  tenantNotificationsEnabled Boolean               @default(true)
  updatedAt                  DateTime              @updatedAt
  updatedBy                  String?
}

model User {
  id                        String                    @id @default(uuid())
  firstName                 String
  lastName                  String
  displayName               String?
  email                     String?                   @unique
  phoneNumber               String?
  notes                     String?
  photoUrl                  String?
  profilePicture            String?
  photos                    Json?
  role                      Role?
  tenantId                  String?
  businessData              Json?
  deletedAt                 DateTime?
  deletedBy                 String?
  createdAt                 DateTime                  @default(now())
  updatedAt                 DateTime                  @updatedAt
  password                  String?
  initialPasswordSet        Boolean                   @default(false)
  emailNotificationsEnabled Boolean                   @default(true)
  marketingEmailsEnabled    Boolean                   @default(false)
  emailVerified             Boolean                   @default(false)
  emailVerificationToken    String?                   @unique
  emailVerificationExpiry   DateTime?
  emailVerifiedAt           DateTime?
  googleId                  String?                   @unique
  authProvider              AuthProvider              @default(LOCAL)
  onboardingCompletedAt     DateTime?
  authEvents                AuthEvent[]
  createdBranches           Branch[]                  @relation("BranchCreatedBy")
  deletedBranches           Branch[]                  @relation("BranchDeletedBy")
  cards                     Card[]
  coffeeCustomerProfile     CoffeeCustomerProfile?
  customerTransactions      CustomerTransaction[]     @relation("CustomerTransactions")
  processedTransactions     CustomerTransaction[]     @relation("ProcessedTransactions")
  eventActors               Event[]                   @relation("EventActors")
  eventMembers              Event[]
  gymMemberProfile          GymMemberProfile?
  gymMemberSubscriptions    GymMemberSubscription[]   @relation("GymMemberSubscriptions")
  deletedGymPlans           GymMembershipPlan[]       @relation("DeletedGymPlans")
  gymUserBranches           GymUserBranch[]
  memberAuditLogs           MemberAuditLog[]          @relation("MemberAuditLogs")
  performedAuditLogs        MemberAuditLog[]          @relation("PerformedAuditLogs")
  createdAssignments        PendingMemberAssignment[] @relation("AssignmentCreators")
  pendingAssignments        PendingMemberAssignment?
  deletedByUser             User?                     @relation("UserDeletions", fields: [deletedBy], references: [id])
  deletedUsers              User[]                    @relation("UserDeletions")
  tenant                    Tenant?                   @relation(fields: [tenantId], references: [id])
}

model GymUserBranch {
  id          String      @id @default(uuid())
  userId      String
  branchId    String
  tenantId    String
  accessLevel AccessLevel @default(STAFF_ACCESS)
  isPrimary   Boolean     @default(false)
  permissions Json?
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt
  branch      Branch      @relation(fields: [branchId], references: [id], onDelete: Cascade)
  tenant      Tenant      @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  user        User        @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, branchId])
  @@index([tenantId])
}

model GymMemberProfile {
  id                       String                      @id @default(uuid())
  userId                   String                      @unique
  tenantId                 String
  externalMemberId         String?
  primaryBranchId          String?
  accessLevel              BranchAccessLevel           @default(ALL_BRANCHES)
  role                     GymRole                     @default(GYM_MEMBER)
  status                   GymMemberSubscriptionStatus @default(ACTIVE)
  emergencyContactName     String?
  emergencyContactPhone    String?
  emergencyContactRelation String?
  medicalConditions        String?
  fitnessGoals             String?
  preferredTrainer         String?
  trainerContactNumber     String?
  gender                   String?
  height                   Int?
  weight                   Int?
  allergies                Json?
  lastVisit                DateTime?
  dateOfBirth              DateTime?
  totalVisits              Int?                        @default(0)
  fitnessLevel             String?
  notifications            Json?
  favoriteEquipment        String?
  averageVisitsPerWeek     Int?
  preferredWorkoutTime     String?
  joinedDate               DateTime?
  membershipHistory        Json?
  profileMetadata          Json?
  deletedAt                DateTime?
  deletedBy                String?
  deletionReason           String?
  deletionNotes            String?
  cardStatus               GymMemberCardStatus?        @default(NO_CARD)
  cardUid                  String?
  cardAssignedAt           DateTime?
  createdAt                DateTime                    @default(now())
  updatedAt                DateTime                    @updatedAt
  primaryBranch            Branch?                     @relation("PrimaryBranchProfiles", fields: [primaryBranchId], references: [id])
  tenant                   Tenant                      @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  user                     User                        @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([tenantId, externalMemberId])
  @@index([tenantId])
}

model CoffeeCustomerProfile {
  id                 String     @id @default(uuid())
  userId             String     @unique
  tenantId           String
  role               CoffeeRole @default(COFFEE_CUSTOMER)
  status             String     @default("active")
  favoriteDrinks     String[]
  dietaryPreferences String?
  loyaltyPoints      Int        @default(0)
  visitHistory       Json?
  createdAt          DateTime   @default(now())
  updatedAt          DateTime   @updatedAt
  tenant             Tenant     @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  user               User       @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([tenantId])
}

model SaasTenantPlan {
  id                String             @id @default(uuid())
  name              String             @unique
  displayName       String
  description       String?
  monthlyPrice      Decimal            @db.Decimal(8, 2)
  yearlyPrice       Decimal?           @db.Decimal(8, 2)
  maxMembers        Int                @default(100)
  maxBranches       Int                @default(1)
  features          Json?
  isActive          Boolean            @default(true)
  createdAt         DateTime           @default(now())
  updatedAt         DateTime           @updatedAt
  saasSubscriptions SaasSubscription[]
}

model GymMembershipPlan {
  id                     String                  @id @default(uuid())
  tenantId               String
  name                   String
  description            String?
  price                  Decimal                 @db.Decimal(8, 2)
  duration               Int
  type                   MembershipType
  benefits               Json?
  accessLevel            BranchAccessLevel       @default(ALL_BRANCHES)
  isActive               Boolean                 @default(true)
  deletedAt              DateTime?
  deletedBy              String?
  deleteReason           String?
  deleteNotes            String?
  createdAt              DateTime                @default(now())
  updatedAt              DateTime                @updatedAt
  gymMemberSubscriptions GymMemberSubscription[]
  deletedByUser          User?                   @relation("DeletedGymPlans", fields: [deletedBy], references: [id])
  tenant                 Tenant                  @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@unique([tenantId, name])
  @@index([tenantId])
  @@index([isActive])
  @@index([deletedAt])
}

model CustomerTransaction {
  id                      String                 @id @default(uuid())
  tenantId                String
  customerId              String
  gymMemberSubscriptionId String?
  businessType            String                 @default("gym")
  transactionCategory     String                 @default("membership")
  amount                  Decimal                @db.Decimal(12, 2)
  currency                String?                @default("PHP")
  taxAmount               Decimal?               @default(0) @db.Decimal(12, 2)
  discountAmount          Decimal?               @default(0) @db.Decimal(12, 2)
  netAmount               Decimal                @db.Decimal(12, 2)
  paymentMethod           String?                @default("cash")
  paymentReference        String?
  gatewayTransactionId    String?
  transactionType         TransactionType        @default(PAYMENT)
  status                  TransactionStatus      @default(COMPLETED)
  relatedEntityType       String?
  relatedEntityId         String?
  relatedEntityName       String?
  description             String?
  notes                   String?
  metadata                Json?
  processedBy             String?
  createdAt               DateTime               @default(now())
  updatedAt               DateTime               @updatedAt
  customer                User                   @relation("CustomerTransactions", fields: [customerId], references: [id], onDelete: Cascade)
  gymMemberSubscription   GymMemberSubscription? @relation(fields: [gymMemberSubscriptionId], references: [id])
  processor               User?                  @relation("ProcessedTransactions", fields: [processedBy], references: [id])
  tenant                  Tenant                 @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  platformRevenue         PlatformRevenue[]

  @@index([tenantId])
  @@index([customerId])
  @@index([gymMemberSubscriptionId])
  @@index([businessType])
  @@index([createdAt])
  @@index([status])
  @@index([amount])
}

model GymMemberSubscription {
  id                   String                      @id @default(uuid())
  tenantId             String
  branchId             String?
  memberId             String
  gymMembershipPlanId  String
  status               GymMemberSubscriptionStatus @default(ACTIVE)
  startDate            DateTime                    @db.Date
  endDate              DateTime                    @db.Date
  price                Decimal                     @db.Decimal(12, 2)
  currency             String?                     @default("PHP")
  usageData            Json?
  cancelledAt          DateTime?
  cancellationReason   String?
  cancellationNotes    String?
  autoRenew            Boolean?                    @default(true)
  nextBillingDate      DateTime?                   @db.Date
  createdAt            DateTime                    @default(now())
  updatedAt            DateTime                    @updatedAt
  customerTransactions CustomerTransaction[]
  branch               Branch?                     @relation(fields: [branchId], references: [id])
  gymMembershipPlan    GymMembershipPlan           @relation(fields: [gymMembershipPlanId], references: [id])
  member               User                        @relation("GymMemberSubscriptions", fields: [memberId], references: [id], onDelete: Cascade)
  tenant               Tenant                      @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@index([tenantId])
  @@index([branchId])
  @@index([memberId])
  @@index([gymMembershipPlanId])
  @@index([status])
  @@index([endDate])
}

model PlatformRevenue {
  id                  String               @id @default(uuid())
  tenantId            String
  tenantName          String
  businessType        String
  revenueType         String
  amount              Decimal              @db.Decimal(12, 2)
  currency            String?              @default("PHP")
  billingPeriodStart  DateTime?            @db.Date
  billingPeriodEnd    DateTime?            @db.Date
  paymentStatus       String?              @default("pending")
  paymentDate         DateTime?
  paymentMethod       String?
  paymentReference    String?
  sourceTransactionId String?
  subscriptionPlanId  String?
  description         String?
  notes               String?
  metadata            Json?
  createdAt           DateTime             @default(now())
  updatedAt           DateTime             @updatedAt
  sourceTransaction   CustomerTransaction? @relation(fields: [sourceTransactionId], references: [id])
  tenant              Tenant               @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@index([tenantId])
  @@index([createdAt])
  @@index([revenueType])
  @@index([paymentStatus])
}

model MemberAuditLog {
  id            String            @id @default(uuid())
  memberId      String
  action        MemberAuditAction
  reason        String?
  notes         String?
  previousState String?
  newState      String?
  performedBy   String?
  performedAt   DateTime          @default(now())
  metadata      Json?
  createdAt     DateTime          @default(now())
  member        User              @relation("MemberAuditLogs", fields: [memberId], references: [id], onDelete: Cascade)
  performer     User?             @relation("PerformedAuditLogs", fields: [performedBy], references: [id])

  @@index([memberId])
  @@index([performedAt])
  @@index([action])
}

model EmailSettings {
  id             String   @id @default(uuid())
  smtpHost       String
  smtpPort       Int
  smtpUser       String?
  smtpPassword   String?
  fromEmail      String
  fromName       String
  brevoApiKey    String?
  mailpitEnabled Boolean  @default(true)
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
}

model EmailTemplate {
  id           String     @id @default(uuid())
  tenantId     String?
  templateType String
  name         String
  subject      String
  htmlContent  String
  textContent  String?
  variables    Json?
  isActive     Boolean    @default(true)
  createdAt    DateTime   @default(now())
  updatedAt    DateTime   @updatedAt
  emailLogs    EmailLog[]
  tenant       Tenant?    @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@index([tenantId])
  @@index([templateType])
  @@index([isActive])
}

model EmailLog {
  id             String         @id @default(uuid())
  tenantId       String?
  recipientEmail String
  recipientName  String?
  templateType   String
  templateId     String?
  subject        String
  status         String
  errorMessage   String?
  sentAt         DateTime?
  provider       String
  metadata       Json?
  createdAt      DateTime       @default(now())
  template       EmailTemplate? @relation(fields: [templateId], references: [id])
  tenant         Tenant?        @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@index([tenantId])
  @@index([templateId])
  @@index([status])
  @@index([createdAt])
}

model SaasSubscription {
  id                 String                 @id @default(uuid())
  businessUnitId     String
  saasPlanId         String?
  planName           String                 @default("basic")
  status             SaasSubscriptionStatus @default(TRIAL)
  startDate          DateTime               @db.Date
  endDate            DateTime?              @db.Date
  trialEndsAt        DateTime?              @db.Date
  monthlyPrice       Decimal                @default(3999) @db.Decimal(8, 2)
  currency           String                 @default("PHP")
  paymentMethod      String?
  paymentReference   String?
  lastPaymentDate    DateTime?
  nextBillingDate    DateTime?              @db.Date
  autoRenew          Boolean                @default(true)
  cancelledAt        DateTime?
  cancellationReason String?
  metadata           Json?
  createdAt          DateTime               @default(now())
  updatedAt          DateTime               @updatedAt
  businessUnit       BusinessUnit           @relation(fields: [businessUnitId], references: [id], onDelete: Cascade)
  saasPlan           SaasTenantPlan?        @relation(fields: [saasPlanId], references: [id])

  @@index([businessUnitId])
  @@index([saasPlanId])
  @@index([status])
  @@index([endDate])
  @@index([nextBillingDate])
  @@index([trialEndsAt])
}

model Terminal {
  id           String        @id @default(uuid())
  gymId        String
  tenantId     String?
  name         String
  secretHash   String
  isActive     Boolean       @default(true)
  lastSeenAt   DateTime?
  createdAt    DateTime      @default(now())
  events       Event[]
  tapCooldowns TapCooldown[]
  dailyEntries DailyEntry[]
  gym          Branch        @relation(fields: [gymId], references: [id], onDelete: Cascade)
  tenant       Tenant?       @relation(fields: [tenantId], references: [id])

  @@index([gymId])
}

model TapCooldown {
  terminalId String
  cardUid    String
  lastTapAt  DateTime
  createdAt  DateTime @default(now())
  terminal   Terminal @relation(fields: [terminalId], references: [id], onDelete: Cascade)

  @@id([terminalId, cardUid])
}

model InventoryCard {
  uid            String              @id
  status         InventoryCardStatus @default(AVAILABLE)
  allocatedGymId String
  batchId        String?
  createdAt      DateTime            @default(now())
  updatedAt      DateTime            @updatedAt
  gym            Branch              @relation(fields: [allocatedGymId], references: [id], onDelete: Cascade)

  @@index([allocatedGymId, status])
}

model Card {
  uid             String   @id
  gymId           String
  type            CardType @default(MONTHLY)
  memberId        String?
  active          Boolean  @default(true)
  isReclaimPending Boolean @default(false)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  gym             Branch   @relation(fields: [gymId], references: [id], onDelete: Cascade)
  member          User?    @relation(fields: [memberId], references: [id])

  @@index([gymId, uid])
}

model PendingMemberAssignment {
  gymId           String            @id @unique
  memberId        String            @unique
  purpose         AssignmentPurpose @default(ONBOARD)
  expiresAt       DateTime
  createdByUserId String?
  createdAt       DateTime          @default(now())
  oldCardUid      String?
  expectedCardUid String?
  createdBy       User?             @relation("AssignmentCreators", fields: [createdByUserId], references: [id])
  gym             Branch            @relation(fields: [gymId], references: [id], onDelete: Cascade)
  member          User              @relation(fields: [memberId], references: [id], onDelete: Cascade)
}

model Event {
  id          String    @id @default(uuid())
  gymId       String
  terminalId  String?
  type        String
  cardUid     String?
  memberId    String?
  actorUserId String?
  meta        Json?
  createdAt   DateTime  @default(now())
  actor       User?     @relation("EventActors", fields: [actorUserId], references: [id])
  gym         Branch    @relation(fields: [gymId], references: [id], onDelete: Cascade)
  member      User?     @relation(fields: [memberId], references: [id])
  terminal    Terminal? @relation(fields: [terminalId], references: [id])

  @@index([gymId, createdAt])
  @@index([type])
}

model DailyEntry {
  id             String           @id @default(cuid())
  gymId          String
  terminalId     String?
  cardUid        String
  occurredAt     DateTime         @default(now())
  amount         Int
  status         DailyEntryStatus @default(RECORDED)
  voidedAt       DateTime?
  voidedByUserId String?
  voidReason     String?
  createdAt      DateTime         @default(now())

  gym            Branch           @relation(fields: [gymId], references: [id], onDelete: Cascade)
  terminal       Terminal?        @relation(fields: [terminalId], references: [id], onDelete: SetNull)

  @@index([gymId, occurredAt])
  @@index([cardUid, occurredAt])
}

model AuthEvent {
  id        String   @id @default(uuid())
  tenantId  String?
  userId    String?
  type      String
  ipAddress String?
  userAgent String?
  reason    String?
  meta      Json?
  createdAt DateTime @default(now())
  tenant    Tenant?  @relation(fields: [tenantId], references: [id])
  user      User?    @relation(fields: [userId], references: [id])

  @@index([tenantId, createdAt])
  @@index([userId, createdAt])
  @@index([type])
}

enum BusinessCategory {
  GYM
  COFFEE_SHOP
  ECOMMERCE
  OTHER
}

enum TenantStatus {
  PENDING
  ACTIVE
  SUSPENDED
  CANCELLED
}

enum Role {
  SUPER_ADMIN
  OWNER
  MANAGER
  STAFF
  CLIENT
}

enum GymRole {
  GYM_TRAINER
  GYM_NUTRITIONIST
  GYM_FRONT_DESK
  GYM_MAINTENANCE
  GYM_MEMBER
}

enum CoffeeRole {
  COFFEE_MANAGER
  BARISTA
  CASHIER
  BAKER
  SHIFT_SUPERVISOR
  COFFEE_CUSTOMER
}

enum EcommerceRole {
  STORE_MANAGER
  PRODUCT_MANAGER
  INVENTORY_MANAGER
  CUSTOMER_SERVICE
  MARKETING_MANAGER
  FULFILLMENT_STAFF
  VENDOR
  ECOM_CUSTOMER
}

enum AccessLevel {
  FULL_ACCESS
  MANAGER_ACCESS
  STAFF_ACCESS
  READ_ONLY
}

enum PlanType {
  TRIAL
  PAID
}

enum BillingCycle {
  TRIAL
  MONTHLY
  YEARLY
  ONE_TIME
}

enum SubscriptionStatus {
  ACTIVE
  INACTIVE
  PAST_DUE
  CANCELED
  EXPIRED
}

enum PaymentStatus {
  PENDING
  SUCCESSFUL
  FAILED
}

enum MembershipType {
  DAY_PASS
  WEEKLY
  MONTHLY
  QUARTERLY
  SEMI_ANNUAL
  ANNUAL
  UNLIMITED
  STUDENT
  SENIOR
  CORPORATE
  CUSTOM
}

enum TransactionType {
  PAYMENT
  REFUND
  ADJUSTMENT
  REVERSAL
  CHARGEBACK
}

enum TransactionStatus {
  PENDING
  PROCESSING
  COMPLETED
  FAILED
  CANCELLED
  REFUNDED
}

enum GymMemberSubscriptionStatus {
  ACTIVE
  INACTIVE
  SUSPENDED
  CANCELLED
  EXPIRED
  PENDING_ACTIVATION
  NO_SUBSCRIPTION
}

enum GymMemberCardStatus {
  NO_CARD
  PENDING_CARD
  ACTIVE
  DISABLED
}

enum MemberAuditAction {
  ACCOUNT_CREATED
  ACCOUNT_ACTIVATED
  ACCOUNT_DEACTIVATED
  ACCOUNT_DELETED
  ACCOUNT_RESTORED
  SUBSCRIPTION_STARTED
  SUBSCRIPTION_RENEWED
  SUBSCRIPTION_CANCELLED
  SUBSCRIPTION_EXPIRED
  SUBSCRIPTION_SUSPENDED
  SUBSCRIPTION_RESUMED
  PAYMENT_RECEIVED
  PAYMENT_FAILED
  PAYMENT_REFUNDED
  PROFILE_UPDATED
  PROFILE_PHOTO_UPDATED
  FACILITY_ACCESS_GRANTED
  FACILITY_ACCESS_REVOKED
  LOGIN_SUCCESSFUL
  LOGIN_FAILED
  CARD_ASSIGNMENT_INITIATED
  CARD_ASSIGNED
  CARD_ASSIGNMENT_FAILED
  CARD_ASSIGNMENT_CANCELLED
}

enum BusinessUnitType {
  LOCATION
  CHANNEL
  DEPARTMENT
  FRANCHISE
}

enum SaasSubscriptionStatus {
  TRIAL
  ACTIVE
  PAST_DUE
  SUSPENDED
  CANCELLED
  EXPIRED
}

enum BranchAccessLevel {
  SINGLE_BRANCH
  MULTI_BRANCH
  ALL_BRANCHES
}

enum PasswordSecurityLevel {
  LOW
  MEDIUM
  HIGH
}

enum InventoryCardStatus {
  AVAILABLE
  ASSIGNED
  DISABLED
}

enum CardType {
  MONTHLY
  DAILY
  SUPER_ADMIN
}

enum DailyEntryStatus {
  RECORDED
  VOIDED
}

enum AssignmentPurpose {
  ONBOARD
  REPLACE
  RECLAIM
}

enum AuthProvider {
  LOCAL
  GOOGLE
}
